I connected the N20 local llama-server endpoint to the provided RAG-style pipeline in 03-milestone-integration/pipeline.py. The retrieval step is currently a toy in-memory keyword retriever rather than my full N19 vector store, but it proves the OpenAI-compatible server can receive assembled context and return an answer. Latency is dominated by the llama-server call; retrieval is effectively 0 ms, while generation takes several seconds on my CPU-only i7-8750H machine.